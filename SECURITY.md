# 安全策略

## 报告安全问题

请不要在公开 Issue 中披露可利用的漏洞、密钥或个人数据。请通过 GitHub 仓库的 Security Advisories 私下报告，并说明影响范围、复现步骤和建议修复方式。

## 部署边界

- 项目通过桌面自动化控制已登录的微信客户端，仅应在你有权使用的账号和设备上运行。
- 管理控制台包含采集、配置和导出能力，必须设置 `CONTROL_PANEL_PASSWORD`，不得直接暴露到公网。
- 控制台如需局域网访问，应通过防火墙限制来源；外网访问应使用 VPN、HTTPS 反向代理和额外身份认证。
- `.env`、MongoDB 连接串、Qwen API Key、Webhook、Cookie、日志、截图与导出数据不得提交到 Git。
- 开源仓库不包含任何生产凭据。部署者负责遵守微信平台规则、适用法律及所在组织的数据管理制度。

## 支持版本

安全修复只保证应用于默认分支的最新版本。发现问题后请先升级并复现。
