# 项目维护约束

- 始终使用简体中文沟通。
- 微信采集执行流程的唯一有效依据是 `docs/WECHAT_SEARCH_COLLECTION_FLOW.md`。
- 标有“已归档”的文档或章节不得用于实现、排障、测试预期或流程推断；除非任务明确要求恢复该文档，并同时移除归档标记、更新当前流程文档和相关测试。
- 修改搜索、标签管理、资料页轮询、文章采集、去重或恢复逻辑时，必须同步更新 `docs/WECHAT_SEARCH_COLLECTION_FLOW.md`。
- Win11 搜索的固定时序为：粘贴搜索词 → 提交搜索 → OpenCV 确认结果页顶部布局 → 保存提交证据 → `Esc`/安全空白点击释放焦点 → 连续两次验证失焦 → 保存 OCR 证据 → 识别结果页。不得在提交前发送 `Esc`，也不得把“发送过按键”当成“联想层已经关闭”。
