# 参与贡献

感谢你改进本项目。提交前请先阅读 `README.md` 和 `SECURITY.md`。

## 开发流程

1. Fork 仓库并创建功能分支。
2. 使用 `setup-env.bat` 创建项目环境。
3. 修改代码时为关键判断和兼容逻辑添加简洁中文注释。
4. 不提交 `.env`、生产账号清单、日志、截图、MongoDB 数据或任何凭据。
5. 运行测试：

   ```powershell
   .\.venv\Scripts\python.exe -m unittest discover -s tests -v
   ```

6. Pull Request 中说明动机、验证方式、界面变化和兼容性影响。

## 变更原则

- 优先使用窗口相对坐标、OCR 与状态验证，避免依赖固定屏幕绝对坐标。
- 涉及文章写入时必须保留去重、标题匹配和账号匹配保护。
- 新增视觉模型能力时必须保持可选，并提供本地 OCR 路径。
- 管理页面与业务 API 必须保留管理员认证，不得新增绕过认证的入口。
